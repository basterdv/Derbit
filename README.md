# Приложение Derbit  🚀

Сервис для высокопроизводительного мониторинга курсов криптовалют с биржи Deribit. Система автоматически собирает данные, сохраняет их в PostgreSQL и предоставляет API для фильтрации истории цен.

---

## 🏗 Архитектура и Design Decisions

При проектировании системы были приняты следующие ключевые решения для обеспечения отказоустойчивости и производительности:

### ⚡️ Обработка задач (Task Management)
*   **Celery Beat:** Вместо бесконечных циклов `while True` используется Celery Beat. Это гарантирует отказоустойчивость: если задача упадет, планировщик запустит её снова по расписанию.
*   **Асинхронность (aiohttp):** Для запросов к API биржи используется `aiohttp` вместо `requests`. Это позволяет опрашивать все тикеры одновременно (параллельно), минимизируя задержку данных.
*   **Async в Celery:** Применен паттерн `asyncio.run()` внутри `shared_task`, что позволяет корректно выполнять современный асинхронный код внутри синхронных воркеров.

### 📊 База данных и API
*   **Тип Timestamp:** Хранение в формате `BigInteger` (UNIX timestamp). Это экономит место и значительно ускоряет фильтрацию в запросах по сравнению со строковыми датами.
*   **Индексация:** Созданы составные индексы по полям `ticker` и `timestamp`, что обеспечивает мгновенную скорость выполнения GET-запросов на эндпоинте `/filter`.
*   **Connection Pooling:** Использование `Depends(get_db)` в FastAPI позволяет эффективно управлять пулом соединений, закрывая сессии сразу после обработки запроса.

### 🛡 Инфраструктура
*   **Разделение ответственности:** API и Worker (Celery) запускаются в разных контейнерах. Это позволяет масштабировать их независимо друг от друга в зависимости от нагрузки.
*   **Безопасность:** Передача учетных данных прокси реализована через `BasicAuth` в слое `aiohttp`.

---

## 📂 Структура проекта

```
deribit_tracker/
├── app/
│   ├── main.py          # FastAPI приложение и эндпоинты
│   ├── database.py      # Модели и подключение к БД
│   ├── celery_app.py    # Конфигурация Celery
│   └── tasks.py         # Асинхронные задачи на получение цен
├── docker-compose.yml   # Оркестрация контейнеров
├── Dockerfile           # Сборка образа приложения
└── requirements.txt     # Зависимости
```
---

## 🚀 Инструкция по развертыванию
### 1. Подготовка
*   Клонируйте репозиторий.
*   Создайте файл .env в корне проекта и укажите параметры подключения:

````
DB_URL=postgresql://user:password@db:5432/deribit_db
REDIS_URL=redis://redis:6379/0
````

### 2. Запуск через Docker (Рекомендуется)
*   Выполните сборку и запуск всех сервисов одной командой:
````BASH
docker-compose up --build
````
После запуска Swagger UI будет доступен по адресу:
👉 http://localhost:8000/docs

### 3. Локальный запуск (Development)
Если вы запускаете компоненты отдельно (например, через WSL):
*   Redis: sudo service redis-server start
*   API: python -m uvicorn app.main:app --reload
*   Celery Worker: celery -A app.celery_app worker --loglevel=info
*   Celery Beat: celery -A app.celery_app beat --loglevel=info









